# Optional: REST API root (legacy). Leave unset for demo catalog unless you use a custom API.
# VITE_API_BASE=https://example.com/api

# Supabase — when both are set, the storefront + admin use Postgres via Supabase (see supabase/schema.sql).
# VITE_SUPABASE_URL=https://xxxx.supabase.co
# VITE_SUPABASE_ANON_KEY=your-anon-key
