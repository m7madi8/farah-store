---
description: Core project conventions — storefront, i18n, and structure
alwaysApply: true
---

# Project Conventions — Nana's Bites / Chef Farah Ammar

## Stack
- **Frontend**: React (Vite) in `react-app/`. Routes: Home (`/`), Product detail (`/product/:slug`), Checkout (`/checkout`), **Admin** (`/admin` — Firebase Auth + Firestore).
- **Data**:
  - **Firebase** (optional): `VITE_FIREBASE_*` vars. See `react-app/firebase/README.md`, `firestore.rules`, and `seed-products.json`. Storefront loads `products`; checkout writes `orders` with embedded `items[]`; staff uses `/admin/login`.
  - **REST** (optional): `VITE_API_BASE` for a custom API.
  - **Mock**: if neither Firebase nor REST is set, built-in demo catalog + local checkout success.

## i18n
- UI is bilingual: **Arabic (ar)** and **English (en)**. Use `useLanguage()` and `t(key)` from `LanguageContext` and `translations.js`. Add new keys in both languages in `react-app/src/data/translations.js`.
- Support RTL: use logical CSS (`margin-inline`, `padding-inline`, `inset-inline-start`) and `[dir="rtl"]` where needed.

## Design
- **Mobile-first**: Default styles target phones; use `@media (min-width: 768px)` and `@media (min-width: 1024px)` to scale up. Shop sections: horizontal scroll on mobile for Dumpling boxes & Sauces; single-column centered for Date balls & Add-ons.
- **Brand**: Purple palette (`--deep-purple`, `--lavender`, `--soft-pink`, `--off-white`). Product cards: rounded, soft shadow, consistent aspect-ratio for images.

## Key paths
- API: `react-app/src/services/api.js` (`fetchProducts`, `fetchProductBySlug`, `submitOrder`).
- Firebase client: `react-app/src/lib/firebase.js`, `firebaseConfig.js`
- Admin: `react-app/src/pages/admin/` — UI primitives `react-app/src/components/ui/`
- Firebase: `react-app/firebase/firestore.rules`, `react-app/firebase/seed-products.json`
- Shop sections in `HomePage.jsx`: boxes, dateBalls (slug `date-balls-chocolate`), sauces, chopsticks. Category meta and section class names: `shop-category-boxes`, `shop-category-sauces`, etc.
